apiVersion: v2
name: control-plane
description: |
  Carbide2 control plane: dashboard (Rails) + Workspace operator. Deployed
  once per cluster. Provisions per-workspace pods (carbide2-server) on demand
  via the Workspace Custom Resource.
type: application
version: 0.3.1
appVersion: "0.3.1-letigre++"
home: https://github.com/fdimitri/carbide2-control
sources:
  - https://github.com/fdimitri/carbide2-control
